<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.w3schools.app">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/w3schools"
        android:label="@string/app_name"
        android:roundIcon="@drawable/w3schools"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".jQueryActivity">

        </activity>
        <activity android:name=".JavaActivity" />
        <activity android:name=".PHPActivity" />
        <activity android:name=".PythonActivity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>